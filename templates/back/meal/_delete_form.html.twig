<form method="post" action="{{ path('app_back_meal_delete', {'id': meal.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce repas ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meal.id) }}">
    <button type="submit" class="btn btn-danger">supprimer</button>
</form>