<form method="post" action="{{ path('app_back_child_delete', {'id': child.id}) }}" onsubmit="return confirm('Voulez vraiment supprimer cette éléments ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="btn">Supprimer</button>
</form>
