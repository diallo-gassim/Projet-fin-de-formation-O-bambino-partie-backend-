{% extends 'base.html.twig' %}

{% block title %}Modifier utilisateur{% endblock %}

{% block body %}


    <h1>Modifier utillisateur</h1>
    
    <div style="display: flex; justify-content: center;">
        <div style="box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.6); padding: 10px;">
    {{ include('back/user/_form.html.twig', {'button_label': 'Modifier'}) }}
    {{ include('back/user/_delete_form.html.twig') }}
        </div>
    </div>

    <a href="{{ path('app_back_user_index') }}"class="btn btn-secondary">revenir à la liste des utilisateurs</a>


{% endblock %}
